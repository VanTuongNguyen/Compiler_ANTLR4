
        procedure minimize(a : integer; b : integer);
        begin
            if a>b then a := b;
        end
        procedure maximize(a : integer; b : integer);
        begin
            if a<b then a := b;
        end
        